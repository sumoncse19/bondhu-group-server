/* eslint-disable no-unused-vars */
export enum Roles {
  ADMIN = 'admin',
  USER = 'user',
  SUPER_ADMIN = 'superAdmin',
}

export enum Sides {
  A = 'a',
  B = 'b',
}

export enum MaritalStatus {
  SINGLE = 'single',
  MARRIED = 'married',
}
